{
  "_readme": "File này commit vào git — KHÔNG chứa connection string thật. Cấu hình thật nạp từ %APPDATA%\\ICare247\\Api\\appsettings.local.json",

  "_connstring_readme": "Giá trị thật đặt ở appsettings.local.json. Config=metadata (ICare247_Config); LiveData=DB nghiệp vụ thật (HT_NguoiDung — login đọc từ đây); Demo=DB dữ liệu thử (giữ cho đỡ nhầm, không nối mặc định); Audit=DB nhật ký RIÊNG (NK_; rỗng=ghi chung LiveData); Redis=cache + Redis Stream audit; Catalog=DB master multi-tenant.",
  "ConnectionStrings": {
    "Config": "",
    "LiveData": "",
    "Demo": "",
    "Audit": "",
    "Redis": "",
    "Catalog": ""
  },

  "_catalog_readme": "ConnectionStrings:Catalog — chuỗi kết nối Catalog DB master (bảng Tenant). Rỗng = chế độ fallback 1 cấu hình cố định (dùng Config/Data ở trên). Catalog:EncryptionKey = AES-256 key base64 giải mã conn string tenant (rỗng = plaintext, chỉ dev). Catalog:BaseDomain = domain gốc để tách subdomain (vd icare247.vn → congtyA.icare247.vn). Đặt thật qua appsettings.local.json.",
  "Catalog": {
    "EncryptionKey": "",
    "BaseDomain": ""
  },

  "Jwt": {
    "Issuer": "https://icare247.vn",
    "Audience": "icare247-api",
    "SecretKey": "PLACEHOLDER_CHANGE_VIA_LOCAL_CONFIG",
    "ExpirationMinutes": 480
  },

  "_cors_readme": "Cors:AllowedOrigins — danh sách origin frontend được phép (vd https://app.icare247.vn). Rỗng: Development tự nhận mọi origin loopback; Production sẽ dừng khởi động. Đặt giá trị thật qua appsettings.local.json.",
  "Cors": {
    "AllowedOrigins": []
  },

  "_dataprotection_readme": "DataProtection:KeyPath — đường dẫn chia sẻ lưu keyring khi chạy nhiều IIS/instance (vd UNC \\\\fileserver\\icare247\\dp-keys). Rỗng = lưu local (dev/1 node). Mọi node phải trỏ cùng path.",
  "DataProtection": {
    "KeyPath": ""
  },

  "_cache_readme": "Cache:Enabled — bật/tắt TOÀN BỘ cache (config form/view/lookup/resource + menu). Mặc định true. Đặt false (thường ở appsettings.Development.json) khi đang chỉnh cấu hình để thay đổi hiện ngay, khỏi xóa cache thủ công. KHÔNG để false ở production.",
  "Cache": {
    "Enabled": true
  },

  "_filestorage_readme": "FileStorage — hệ đính kèm. Provider=backend cho file LỚN: Db (bytes trong DB, luôn node-safe) | FileSystem (đĩa/shared mount) | Object (MinIO/S3/Azure — chưa cắm SDK). File nhỏ (<=DbThresholdBytes) LUÔN vào DB bất kể Provider. BaseRoot=gốc chứa tuyệt đối khi FileSystem (vd \\\\nas01\\icare247 hoặc D:\\ICare247Files) — MỌI NODE sau load-balancer phải trỏ CÙNG một giá trị; di dời nơi chứa = chỉ đổi giá trị này (đường dẫn trong DB là tương đối nên không đổi). SiteKey=đoạn cô lập vật lý tùy chọn khi nhiều site chung 1 gốc. MaxBytes=giới hạn 1 tệp. Đặt BaseRoot/Object thật qua appsettings.local.json.",
  "FileStorage": {
    "Provider": "Db",
    "BaseRoot": "",
    "SiteKey": "",
    "DbThresholdBytes": 262144,
    "MaxBytes": 52428800,
    "Object": {
      "Endpoint": "",
      "Bucket": "",
      "AccessKey": "",
      "SecretKey": ""
    }
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.Hosting": "Information",
        "System": "Warning"
      }
    }
  },

  "AllowedHosts": "*"
}
