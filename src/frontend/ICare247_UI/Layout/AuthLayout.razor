@inherits LayoutComponentBase

@*
    AuthLayout — layout trần cho nhóm màn xác thực (đăng nhập, quên mật khẩu).
    Không sidebar/topbar; trang con tự dựng bố cục split 2 cột (design-login-hitech).
*@

<div class="auth-shell">
    @Body
</div>
