@inherits LayoutComponentBase

@*
    AuthLayout — layout trần cho nhóm màn xác thực (đăng nhập, quên/đặt lại mật khẩu).
    Không sidebar/topbar; trang con dùng <AuthShell> tự dựng bố cục split 2 cột
    (design-login-hitech). Layout chỉ là passthrough.
*@

@Body
