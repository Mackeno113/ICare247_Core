<UserControl x:Class="ConfigStudio.WPF.UI.Modules.Forms.Views.CloneFormDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Width="440"
             mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- Nút hủy: outline trắng -->
        <Style x:Key="BtnCancel" TargetType="Button">
            <Setter Property="Padding"         Value="20,6" />
            <Setter Property="Cursor"          Value="Hand" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="Foreground"      Value="{DynamicResource AppTextSecondaryBrush}" />
            <Setter Property="Background"      Value="White" />
            <Setter Property="BorderBrush"     Value="{DynamicResource AppBorderStrongBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppSurfaceMutedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Nút clone: xanh dương -->
        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Padding"         Value="20,6" />
            <Setter Property="Cursor"          Value="Hand" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="FontWeight"      Value="Medium" />
            <Setter Property="Foreground"      Value="White" />
            <Setter Property="Background"      Value="{DynamicResource AppAccentBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppAccentLightBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppAccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AppAccentDarkBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Border Background="White"
            BorderBrush="{DynamicResource AppBorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- ── Header ─────────────────────────────────────────── -->
            <Border Grid.Row="0" Background="{DynamicResource AppAccentBrush}" Padding="20,14">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⧉" FontSize="16" Foreground="White"
                               VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBlock Text="Nhân bản Form"
                               FontSize="15" FontWeight="SemiBold" Foreground="White"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- ── Body ───────────────────────────────────────────── -->
            <StackPanel Grid.Row="1" Margin="20,18,20,14">

                <!-- Mô tả -->
                <TextBlock FontSize="13" Foreground="{DynamicResource AppTextSecondaryBrush}" TextWrapping="Wrap">
                    <Run Text="Tạo bản sao của form " />
                    <Run FontWeight="Bold" Text="{Binding SourceFormCode, Mode=OneWay}" />
                    <Run Text=". Sections, Fields và Events sẽ được nhân bản sang form mới." />
                </TextBlock>

                <!-- Label -->
                <TextBlock Margin="0,16,0,4" FontSize="12" FontWeight="SemiBold"
                           Foreground="{DynamicResource AppTextSecondaryBrush}" Text="Form_Code mới *" />

                <!-- Input — dùng DevExpress TextEdit cho nhất quán -->
                <dxe:TextEdit EditValue="{Binding NewFormCode, UpdateSourceTrigger=PropertyChanged}"
                              NullText="VD: PO_ORDER_V2"
                              CharacterCasing="Upper"
                              MaxLength="100" />

                <!-- Gợi ý format -->
                <TextBlock Margin="0,4,0,0" FontSize="11" Foreground="{DynamicResource AppTextSubtleBrush}"
                           Text="Chỉ dùng chữ HOA, số và dấu gạch dưới (A-Z, 0-9, _)." />

                <!-- Validation error -->
                <Border Margin="0,10,0,0" Background="{DynamicResource AppDangerSoftBrush}" BorderBrush="{DynamicResource AppDangerLightBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10,7"
                        Visibility="{Binding HasValidationError, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding ValidationMessage}"
                               FontSize="12" Foreground="{DynamicResource AppDangerBrush}" TextWrapping="Wrap" />
                </Border>

                <!-- Info box -->
                <Border Margin="0,10,0,0" Background="{DynamicResource AppAccentSoftBrush}" BorderBrush="{DynamicResource AppAccentLightBrush}"
                        BorderThickness="1" CornerRadius="4" Padding="10,7">
                    <TextBlock FontSize="12" Foreground="{DynamicResource AppAccentDarkBrush}"
                               Text="Form mới sẽ có Version = 1 và trạng thái Active." />
                </Border>

            </StackPanel>

            <!-- ── Footer ─────────────────────────────────────────── -->
            <Border Grid.Row="2" Background="{DynamicResource AppSurfaceMutedBrush}" BorderBrush="{DynamicResource AppBorderBrush}"
                    BorderThickness="0,1,0,0" Padding="20,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Hủy"
                            Style="{StaticResource BtnCancel}"
                            Margin="0,0,8,0"
                            Command="{Binding CancelCommand}" />
                    <Button Content="Clone"
                            Style="{StaticResource BtnPrimary}"
                            Command="{Binding CloneCommand}" />
                </StackPanel>
            </Border>

        </Grid>
    </Border>
</UserControl>
